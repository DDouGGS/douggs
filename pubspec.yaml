name: douggs
description: Aplicativo Flutter — adiciona tela WebView para douggs.com.br
publish_to: 'none'
environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  webview_flutter: ^4.0.7

flutter:
  uses-material-design: true
